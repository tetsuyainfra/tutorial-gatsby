import React from "react"

import Header from '../components/header'

export default () => (
  <div style={{ color: `teal` }}>
    <Header headerText="About Gatsby">About</Header>
    <Header headerText="It's cool">About</Header>
    <p>Such wow. Very React.</p>
  </div>
)